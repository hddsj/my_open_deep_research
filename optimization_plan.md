# Deep Research Agent 优化计划

基于《The Hitchhiker's Guide to Agentic AI》目录，结合项目现状制定。

## 当前已完成

- [x] 本地知识库（PDF提取 + ChromaDB + BM25 + Reranker）
- [x] 语义分块 + 跨页合并 + chunk 大小控制
- [x] 增量索引 + 多书并行
- [x] 查询改写 + 搜索结果去重
- [x] 研究记忆（存储 + 检索 + 相关度过滤 + 去重）
- [x] ChromaDB 客户端 / Embedding 函数复用
- [x] 日志系统（print → logging）

---

## 优化优先级

### P0 - 生产稳定性（立即做）

| # | 优化项 | 书中章节 | 说明 |
|---|--------|---------|------|
| 1 | **错误容错** | - | memory 操作加 try-except，失败不影响主研究流程。3分钟搞定，防止生产事故 |

### P1 - 记忆系统升级（核心竞争力）

| # | 优化项 | 书中章节 | 说明 |
|---|--------|---------|------|
| 2 | **记忆冲突合并** | 17.4.3 Update: Conflict Resolution | 当前去重是"删旧存新"。升级为：用 LLM 将旧记忆和新记忆合并成一条更完整的摘要，避免信息丢失 |
| 3 | **记忆反思** | 17.4.4 Reflect: Meta-Cognitive | 研究多次后，让 LLM 对已有记忆做元认知反思，提取更高层洞察（如"用户关注容器化技术"） |
| 4 | **分层摘要记忆** | 17.3.2 Summarization-Based Memory | 多条相关记忆自动归纳成上层摘要，形成"具体记忆 → 主题摘要 → 领域洞察"的层级结构 |
| 5 | **会话连续性** | 17.5.2 Session Continuity | 跨会话保持研究上下文，用户关闭浏览器后再回来能继续之前的研究 |
| 6 | **记忆过期机制** | 17.4.3 | 旧记忆按时间衰减，避免过时信息干扰当前研究 |

### P2 - RAG 检索质量提升

| # | 优化项 | 书中章节 | 说明 |
|---|--------|---------|------|
| 7 | **Corrective RAG** | 16.5.5 CRAG | 检索结果质量差时自动修正查询重新检索，而不是直接用低质量结果 |
| 8 | **上下文压缩** | 16.5.3 Contextual Compression | 检索结果送给 LLM 前先压缩，去掉无关段落，只保留与查询相关的核心内容 |
| 9 | **Adaptive RAG** | 16.5.6 | 根据查询类型动态选择检索策略：简单问题直接检索，复杂问题多轮检索+推理 |
| 10 | **Parent-Child Chunking** | 16.4.4 | 检索时匹配小chunk（精确），返回时给大chunk（完整上下文），兼顾精度和完整性 |

### P3 - 评估体系

| # | 优化项 | 书中章节 | 说明 |
|---|--------|---------|------|
| 11 | **检索质量评估** | 16.8.1 Retrieval Metrics | 用 MRR、NDCG 等指标量化知识库检索效果，找到优化方向 |
| 12 | **报告质量评估** | 14.7 LLM-as-Judge | 用 LLM 自动评估最终报告的完整性、准确性、引用质量 |
| 13 | **Agent 效率指标** | 14.6.2 Trajectory Efficiency | 衡量 agent 用了多少步完成研究，优化冗余步骤 |

### P4 - 架构升级（长期）

| # | 优化项 | 书中章节 | 说明 |
|---|--------|---------|------|
| 14 | **Multi-Source Routing** | 16.7.3 | 根据查询自动选择最佳数据源（本地知识库 vs 网络搜索 vs 记忆），而非全部搜一遍 |
| 15 | **Graph-Based Memory** | 17.3.3 | 用知识图谱存储记忆之间的关系，支持更复杂的推理（如"Docker网络"和"K8s网络"的关联） |
| 16 | **Tool-Augmented RAG** | 16.7.5 | 让 agent 根据需要动态调用不同工具获取信息，而非固定的检索流程 |

---

## 执行原则

1. 每个优化项做完后提交到对应的 feature 分支
2. 优先做改动小、收益大的项（P0 → P1 → P2）
3. 每个优化项做前先讲思路，确认后再动手
4. 做完后跑 agent 验证效果
